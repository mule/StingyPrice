@using StingyPriceDAL.Models
 @model   StingyPrice.Models.ViewModels.CategoriesViewModel
@{
    Layout = "_StingyMasterLayout.cshtml";
}
@section SearchBox{
@using (Html.BeginForm("Search", "Categories"))
{

    @Html.TextBox("txtSearchStr")
    
}
}
@section CategoryMenuBox {
    @{


        var menu = Html.Telerik().Menu()
            .Name("MainMenu").BindTo(Model.Root.SubCategories, mappings => mappings.For<Category>(binding => binding
                                                                                                                 .ItemDataBound((item, category) =>
                                                                                                                                    {
                                                                                                                                        item.Text = category.Name;
                                                                                                                                    })
                                                                                                                 .Children(category => category.SubCategories)));


        menu.Render();
    
    }
}
@RenderBody(); 